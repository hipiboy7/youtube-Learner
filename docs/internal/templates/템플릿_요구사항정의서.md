# P<N> 요구사항정의서 — <Topic>

- 상위 문서: `docs/scope-definition.md` (v<k>) <n>절, `docs/설계서_Architecture.md` <n>절
- 사용 프롬프트: `docs/prompts/phase<N>/<산출물>-v<k>.md`
- 규칙: `CLAUDE.md` 4절 1단계 산출물. 다음 산출물은 `docs/P<N>_설계서_<Topic>.md`
- 선행 검증: `docs/internal/P<N>_검토서_TechSpike.md` (프롬프트 작성 전 실측)
- 작성일: YYYY-MM-DD / 작성 LLM: <모델>
- 상태: 초안 | 승인(YYYY-MM-DD) | 개정 v<k>

> 안내: 이 문서는 "무엇을, 왜"만 답한다. "어떻게"(모듈 구조·시그니처·라이브러리 선택)는 설계서에 쓴다.
> 모든 요구사항에는 **근거**(상위 문서 절번호 또는 TechSpike 실측)가 있어야 한다. 근거를 쓸 수 없으면 요구사항을 다시 본다.

## 1. 목적 (Why)

<이 Phase가 없으면 무엇이 안 되는가. 2~5줄.>

## 2. 배경 및 제약

- <실행 환경 제약 — 예: 물리 코어 1개, 디스크 여유 5GB → 설계에 미치는 영향>
- <외부 서비스 제약 — TechSpike에서 실측한 사실. 확인 일자>
- <이전 Phase에서 인계된 사항 — `P<N-1>_테스트결과서` 8절>

## 3. 범위

### In Scope

| # | 항목 | 근거 |
|---|---|---|
| 1 | <항목> | `scope-definition.md` <n>절 |

### Out of Scope (다른 Phase 책임)

| 항목 | 담당 Phase | 근거 |
|---|---|---|
| <항목> | Phase <M> | `scope-definition.md` 7절 로드맵 |

> 안내: 리뷰 반영 시 **가장 자주 어긋나는 표**다. 본문을 고치면 이 표도 반드시 같이 본다 (`CLAUDE.md` 4절).

## 4. 기능 요구사항

> 안내: 모듈(파일) 단위로 하위 절을 나눈다. 설계서·코드 docstring이 같은 모듈명으로 이 절을 역참조한다.
> 번호대: P0 = FR-1~, P1 = FR-101~, P2 = FR-201~ …

### 4.1 `<module>.py` — <역할>

**FR-<n>.** <한 문장. "~해야 한다" 꼴. 측정·검증 가능한 표현으로.>

| 값 | 근거 |
|---|---|
| <구체 값·조건> | <상위 문서 절번호 / TechSpike 3절 실측> |

**FR-<n+1>.** <문장>

| 값 | 근거 |
|---|---|
| … | … |

## 5. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 테스트 등급 | A(순수 로직): <모듈 목록> ≥ 90% / B(오케스트레이션·IO): <모듈 목록> ≥ 70% / C: <검증 스크립트> |
| 테스트 배치 | A → `backend/tests/unit/`, B → `backend/tests/integration/`, 실호출 → `backend/tests/e2e/`(`-m e2e`) — 파일 1:1 |
| 추적성 | 문서 → 코드 경로, 코드 docstring → 문서 절 (`CLAUDE.md` 4절) |
| 하드코딩 금지 | `.env`(환경별) / `constants.py`(설계 고정) / `backend/config/*.json`(실행별) — `CLAUDE.md` 5절 |
| 멱등성 | 같은 입력 재실행 → 산출물·외부 상태 동일. 자연키(`yt_video_id`)로 중복 방지 — `CLAUDE.md` 6절 |
| 성능(해당 시) | <수치 목표와 측정 방법. 없으면 "이 Phase에서 목표 없음 — 측정만 한다"> |
| 문서 검증 | `scripts/verify_docs.py` 종료 코드 0 (pytest 포함) |

## 6. 산출물

| 산출물 | 종류 | 경로 |
|---|---|---|
| 요구사항정의서 | 문서 | `docs/P<N>_요구사항정의서_<Topic>.md` |
| 설계서 | 문서 | `docs/P<N>_설계서_<Topic>.md` |
| <모듈> | 코드 | `backend/src/youtube_learner/<pkg>/<module>.py` |
| <테스트> | 테스트 | `backend/tests/unit/test_<module>.py` |
| <설정> | 설정 | `backend/config/<name>_default.json` |
| 테스트결과서 | 문서 | `docs/P<N>_테스트결과서_<Topic>.md` |

## 7. 완료 기준 (Acceptance Criteria)

> 안내: 체크박스 목록. 테스트결과서 7절이 이 목록을 그대로 복제해 `[x]`로 대조한다. 기계로 판정할 수 있는 문장으로 쓴다.

- [ ] <FR 전건에 대응하는 테스트가 존재하고 통과한다 (테스트결과서 4절)>
- [ ] <등급 A 커버리지 ≥ 90%, B ≥ 70% 실측 기록>
- [ ] <실연동 확인: 명령·실제 출력·확인 일자가 테스트결과서 5절에 있다>
- [ ] <멱등성: 같은 입력 2회 실행 결과 동일 — 확인 명령>
- [ ] `scripts/verify_docs.py` 종료 코드 0
- [ ] <Phase 고유 기준 …>

## 8. 리스크 및 참고사항

| # | 리스크 | 대응 | 확인 시점 |
|---|---|---|---|
| 1 | <리스크> | <대응> | <Phase/단계> |

## 9. 보류 결정 등재 후보

> 안내: 이 Phase에서 "지금 결정하지 않는다"로 넘기는 것. Phase 종료 시 `CLAUDE.md` 1절 보류 결정 표에 (a)트리거 (b)기계 판정법 (c)이정표 3요소로 등재한다.

| 결정 | 왜 지금 안 하나 | 트리거 | 판정법 |
|---|---|---|---|
| <결정> | <이유> | <Phase/사건> | <명령·조건> |

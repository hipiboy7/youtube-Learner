# P<N> 설계서 — <Topic>

- 상위 문서: `docs/scope-definition.md` <n>절, `docs/설계서_Architecture.md` <n>절
- 규칙: `CLAUDE.md` 4절 1단계 산출물(요구사항 + 설계). 다음 산출물은 코드+테스트 → `docs/internal/P<N>_실측기록_<Topic>.md`
- 실측 근거: `docs/internal/P<N>_실측기록_<Topic>.md` (해당 시), `docs/internal/검토서_Prototype.md` (Phase 1~3)
- 작성일: YYYY-MM-DD / 작성 LLM: <모델>
- 상태: 초안 | 확인(YYYY-MM-DD) | 개정 v<k>

> 안내: **Phase 당 문서 3개 중 첫 번째.** 요구사항(무엇을·왜)과 설계(어떻게)를 한 문서에 담는다 — 두 문서로 나누면 모듈 목록을 두 번 쓴다.
> **단일 출처 규칙**(`CLAUDE.md` 5절): 실행 파라미터·실측값을 본문에 옮겨 적지 않는다. 설정 파일명과 실측기록 절 번호로 링크한다.
> `설계 판단` 절이 이 문서의 핵심이다 — 모든 결정을 **왜 이것인가 + 기각한 대안 + 기각 이유**로 쓴다. 이유를 쓸 수 없으면 그 결정을 다시 본다.

## 1. 목적 (Why)

<이 Phase 가 없으면 무엇이 안 되는가. 2~5줄.>

## 2. 배경 및 제약

- <실행 환경 제약 — 자원·속도. 값은 실측기록 절 링크>
- <외부 서비스 제약 — 프로토타입·실측에서 확인한 사실. 확인 일자>
- <이전 Phase 인계 사항 — `P{N-1}_실측기록` 인계 절>

## 3. 범위

### In Scope

| # | 항목 | 근거 |
|---|---|---|
| 1 | <항목> | `scope-definition.md` <n>절 |

### Out of Scope (다른 Phase 책임)

| 항목 | 담당 Phase | 근거 |
|---|---|---|
| <항목> | Phase <M> | `scope-definition.md` 7절 |

> 리뷰 반영 시 **가장 자주 어긋나는 표**다. 본문을 고치면 이 표도 반드시 같이 본다.

## 4. 요구사항 (FR)

> 번호대: P0 = FR-1~99, P1 = FR-101~199, P2 = FR-201~299 … 모듈(파일) 단위로 하위 절을 나눈다.
> 코드 docstring 과 테스트가 이 번호를 역참조한다.

### 4.1 `<module>.py` — <역할>

**FR-<n>.** <한 문장. "~해야 한다" 꼴. 측정·검증 가능한 표현으로.>

| 값 | 근거 |
|---|---|
| <구체 값·조건> | <상위 문서 절번호 / 실측기록 절> |

## 5. 모듈 구성 및 의존 방향

```
<패키지 트리 + 등급(A/B) 주석 + 의존 방향 한 줄>
```

## 6. `<module>.py` — <역할>

**대응**: FR-<n>, FR-<n+1>

### 6.1 인터페이스

```python
def fn(arg: Arg) -> Result:
    """<한 줄 역할>. 등급 A.

    Raises:
        SomeError: <조건>
    """
```

| 입력 | 출력 | 예외 | 부작용 |
|---|---|---|---|
| <타입·제약> | <타입> | <조건 → 예외> | 없음(순수) / <파일·DB·네트워크> |

### 6.2 설계 판단

- **왜 <선택>인가** — <이유. 상위 문서·실측 근거>. → 대안 A는 <이유>로 기각, B는 <이유>로 기각.
- **왜 경계를 여기에 두나** — <변경 가능성이 높은 축이 무엇인지>.

### 6.3 검증 방법

| 등급 | 테스트 파일 | 확인하는 것 |
|---|---|---|
| A | `backend/tests/unit/test_<module>.py` | 정상 + **거부 케이스** |

## 7. 데이터 (해당 시)

| 테이블/파일 | 키 | 필드 | 보존 규칙 |
|---|---|---|---|
| <name> | <자연키> | <필드…> | <원본 보존 / 재생성 가능 / 버전 동반> |

## 8. 설정값 배치 (단일 출처 — CLAUDE.md 5절)

| 값 | 위치 | 이유 |
|---|---|---|
| <값> | `.env` / `constants.py` / `backend/config/<name>.json` | <환경별 / 설계 고정 / 실행별> |

## 9. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 테스트 등급 | A(≥90%): <모듈> / B(≥70%): <모듈> / C: <스크립트> |
| **E2E 스모크** | <이 Phase 가 완성한 흐름 한 줄> — `backend/tests/e2e/`, `-m e2e` |
| 멱등성 | <같은 입력 재실행 시 무엇이 같아야 하는가> |
| 추적성 | 모듈 docstring 첫 줄에 이 문서 절·FR·등급 |
| 단일 출처 | 실행 파라미터는 설정 파일에만, 실측값은 실측기록에만 |

## 10. 구현 순서

| 순서 | 작업 | 등급 | 테스트 먼저? | 산출물 |
|---|---|---|---|---|
| 1 | <A등급 순수 로직> | A | 예 | `<경로>` |
| 2 | <B등급 오케스트레이션> | B | 아니오 | `<경로>` |
| 3 | E2E 스모크 | — | — | `backend/tests/e2e/` |

## 11. 완료 기준 (Acceptance Criteria)

> 체크박스. 실측기록의 "완료 기준 대조" 절이 이 목록을 복제해 `[x]` 로 대조한다. **기계로 판정할 수 있는 문장**으로 쓴다.

- [ ] FR 전건에 대응하는 테스트가 존재하고 통과한다
- [ ] 등급 A ≥ 90%, B ≥ 70%, A+B ≥ 80% (실측기록에 재현 명령)
- [ ] E2E 스모크 <시나리오> 가 실제 호출로 통과하고 출력이 실측기록에 있다
- [ ] 멱등성: <확인 명령>
- [ ] `scripts/verify_docs.py` 종료 코드 0
- [ ] <Phase 고유 기준>

## 12. 리스크

| # | 리스크 | 대응 | 확인 시점 |
|---|---|---|---|
| 1 | <리스크> | <대응> | <Phase/단계> |

## 13. ⚠️ 다음 Phase 인계

| 항목 | 내용 | 받는 Phase |
|---|---|---|
| <인터페이스/데이터/미결> | <내용> | Phase <M> |

## 14. 보류 결정 등재 후보

| 결정 | 왜 지금 안 하나 | 트리거 | 기계 판정법 |
|---|---|---|---|
| <결정> | <이유> | <Phase/사건> | <명령·조건> |

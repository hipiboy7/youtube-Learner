# P<N> 테스트결과서 — <Topic>

- 상위 문서: `docs/P<N>_요구사항정의서_<Topic>.md` (v<k>) 7절, `docs/P<N>_설계서_<Topic>.md` (v<k>)
- 규칙: `CLAUDE.md` 4절 4단계 산출물. 다음은 `docs/internal/P<N>_검토서_SelfReview.md` (1절 6단계)
- 실행일: YYYY-MM-DD / 작성 LLM: <모델> / 브랜치: `impl-phase<N>` / 커밋: `<hash>`
- 결과: **전 항목 통과 (PASS)** | 일부 실패 (FAIL <n>건 — 6절)

> 안내: 숫자는 전부 **실측**이다. 커버리지 표는 `pytest --cov` 출력을 복사한다. 기억으로 쓰지 않는다.
> 실패·재작업도 기록한다 — 무엇이 한 번에 안 됐는지가 다음 Phase의 정보다.

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| OS | <Windows Server 2022 …> |
| CPU / RAM / 디스크 여유 | <실측> |
| Python | <3.12.x> (`backend\.venv\Scripts\python.exe`) |
| Node | <v24.x> |
| 테스트 도구 | pytest <ver>, pytest-cov <ver> / vitest <ver> |
| 외부 서비스 상태 | `scripts/check_env.ps1` → `==> READY` (YYYY-MM-DD HH:MM) |
| 주요 패키지 | yt-dlp <ver>, faster-whisper <ver>, ctranslate2 <ver> |

## 2. 테스트 결과 요약

| 구분 | 테스트 수 | 결과 |
|---|---|---|
| 등급 A (`backend/tests/unit/`) | <n> | PASS |
| 등급 B (`backend/tests/integration/`) | <n> | PASS |
| 프론트 (`frontend/`, vitest) | <n> | PASS / 해당 없음 |
| E2E (`backend/tests/e2e/`, `-m e2e`, 수동) | <n> | PASS (5절) / 미실행 — 이유 |
| **합계 (기본 pytest)** | **<n>** | **PASS** |

## 3. 등급별 커버리지

### 3.1 등급 A — 목표 ≥ 90%

| 모듈 | Stmts | Miss | Branch | BrPart | Cover |
|---|---|---|---|---|---|
| `<pkg>/<module>.py` | | | | | |
| **소계** | | | | | **<n>%** → 목표 대비 +<k>%p |

### 3.2 등급 B — 목표 ≥ 70%

| 모듈 | Stmts | Miss | Branch | BrPart | Cover |
|---|---|---|---|---|---|
| `<pkg>/<module>.py` | | | | | |
| **소계** | | | | | **<n>%** → 목표 대비 +<k>%p |

### 3.3 A+B 가중 평균 — 목표 ≥ 80%

<n>% (Stmts 가중). 산출: (<A stmts>×<A%> + <B stmts>×<B%>) / (<A+B stmts>)

### 3.4 측정 제외

| 대상 | 이유 |
|---|---|
| `domain/interfaces.py` | Protocol 선언만 — 실행 로직 없음 (`pyproject.toml` omit) |
| `scripts/spike/*` | 등급 C — 산출물 검증 스크립트 |

## 4. 요구사항 대응 검증

> 안내: 요구사항정의서 4절 FR **전건**을 1행씩. 검증 방법에는 테스트 클래스·함수명까지. 자동 테스트가 없으면 "수동 — 5절 <n>" 또는 "문서 — <경로>".

| FR | 내용(요약) | 검증 방법 | 결과 |
|---|---|---|---|
| FR-<n> | <요약> | `test_<module>.py::Test<Class>::test_<name>` | PASS |
| FR-<n+1> | <요약> | 수동 실호출 — 5.1절 | PASS |

## 5. 실연동 검증 (YouTube · Whisper · 기타)

> 안내: 모킹 테스트만으로는 완료가 아니다 (`CLAUDE.md` 8절). 명령을 **적은 그대로** 실행하고 **실제 출력을 복사**한다. 확인 일자 필수.

### 5.1 <대상 — 예: yt-dlp 탭 목록>

```powershell
<실행한 명령 그대로>
```

```
<실제 출력 — 핵심 부분>
```

확인 일자: YYYY-MM-DD, yt-dlp <ver>. 소요: <n>초. 건수: <n>.

## 6. 재작업·특이사항

### 6.1 <발견 사항 한 줄> (구현 중 발견) ⚠️

- 증상: <실제 출력>
- 원인: <원인>
- 조치: <조치> (커밋 `<hash>`)
- 기록: `docs/internal/검토서_트러블슈팅.md` T-<0NN>

## 7. 완료 기준 대조 (요구사항정의서 7절)

> 안내: 요구사항정의서 7절 목록을 **그대로 복제**하고 `[x]`로 바꾼다. 미충족이 있으면 `[ ]`로 두고 이유·계획을 적는다.

- [x] <기준 1> — 근거: 4절 / 3.1절
- [x] <기준 2> — 근거: 5.1절
- [ ] <미충족 기준> — 이유: <…>. 계획: <Phase/일자>

## 8. 다음 Phase 인계 사항

| 항목 | 내용 | 받는 Phase |
|---|---|---|
| <인터페이스 변경/미결/데이터> | <내용> | Phase <M> |

## 9. 재현 명령

> 안내: 독자가 이 결과를 다시 얻는 명령. `scripts/verify_docs.py`가 경로·모듈 존재를 검사한다.

```powershell
.\scripts\check_env.ps1
.\backend\.venv\Scripts\python.exe -m pytest --cov=youtube_learner --cov-report=term
.\backend\.venv\Scripts\python.exe scripts\verify_docs.py
```

기대값: pytest <n> passed, 종료 코드 0, 소요 약 <n>초.

# P0 질의응답 — 디스크 정책과 수동 요약 흐름

- 일자: 2026-09-14 / 참여: 사용자, Fable 5.1
- 관련: `CLAUDE.md` 7절(디스크 D: 우선)·1절 보류 결정 8, `docs/scope-definition.md` v2 2.4절·8.1절·9절, `docs/internal/qa/P0_질의응답_초기요구.md`

## Q1. (Claude → 사용자, Phase 0 요약에서) 디스크 여유가 C 4.17GB / D 4.09GB 라 Phase 4 의 Rust 툴체인·빌드 산출물이 들어갈 자리가 없다. 증설이나 정리가 필요하다.
A. (사용자) "로컬 디스크를 사용할 때는 항상 D 드라이브를 우선적으로 사용하도록 해." / "phase 가 진행됨에 따라 디스크가 필요하면 D 드라이브를 증설하도록 할게."
→ 결정: **규칙** — 저장소·venv·`node_modules`·`DATA_DIR`·모델 캐시·도구·pip/npm 캐시 전부 D:, C: 는 OS 전용, `~` 기준 기본 경로 금지(`CLAUDE.md` 7절). **보류 결정 8 의 해법** — 부족하면 D: 증설(트리거 P4 착수, P2 에서 모델 2개 이상 보유 시 조기). 즉시 조치 — bgutil 을 홈(C:)→`tools/`(D:), pip/npm 캐시 위치 `D:\claude\.cache\`, C: 의 pip(116MB)·npm(321MB)·HF 캐시 삭제 → C 4.78GB / D 3.90GB.

## Q2. (사용자) 요약·핵심 도출은 추후 구현이지만, 지금은 사용자가 스크립트를 직접 복사해 각자 쓰는 AI 챗 서비스에 붙이고, 받은 요약·핵심 정리를 다시 붙여 넣어 저장할 수 있어야 한다. 스크립트 추출 후 버튼 하나로 '복사', 외부에서 정리한 글을 붙일 '요약 및 정리' 메뉴와 저장 기능.
A. (Claude) 자동 분석(P5)은 그대로 두고 **수동 흐름을 v1(P3)에 넣는다.** 데이터는 자동 분석기와 같은 `analyses` 테이블에 `analyzer_name="manual"`, `analyzer_version="user"` 로 저장해 P5 가 붙어도 한 화면에서 출처로 구분한다. 수동 입력은 계산이 없으니 `Analyzer` 레지스트리를 거치지 않는다. 복사는 전체 텍스트 원클릭이며, 타임스탬프 포함 여부·요약 요청 프롬프트 머리말 포함 여부는 P3 설계에서 정한다.
→ 결정: scope v2 (In-Scope 10, 6.1 성공 기준, P3 로드맵), `CLAUDE.md` 0절, Architecture 4·5.2절, `constants.MANUAL_ANALYZER_NAME/VERSION`. P1 은 `analyses` 스키마 생성, P3 는 API(`POST /videos/{id}/analyses` 수동 201)·화면.

## Q3. (Claude, 미결) 복사 버튼이 클립보드에 넣을 형식 — 텍스트만 / 타임스탬프 포함 / "다음 스크립트를 요약해줘" 같은 프롬프트 머리말 포함 옵션?
A. 사용자 답 없음 — P3 요구사항 도출 시 질문한다(기본안: 텍스트만 + 옵션 2개 토글).
→ 결정: 미정. P3 프롬프트의 질문 항목.

# 작업 히스토리: 프로토타입 마무리(PO 토큰 상주 서버) + 방법론 v2 개정

- 일자: 2026-09-16
- 작업자: park.sei (LLM: Opus 5)
- 브랜치: `prototype/e2e-slice`(프로토타입 수정) → `chore/methodology-v2`(방법론 개정, worktree `D:\claude\ytl-method`)
- 이전 세션: `history/2026-09-15_park.sei_prototype-e2e.md`

## 1. 프로토타입 마무리

어제 남은 사용자 확인을 이어받아 서버를 재기동하던 중 자막 경로가 다시 실패했다. 어제 우회했던 것(T-006)의 뿌리였다.

- **원인**: PO 토큰 **script 모드**가 요청마다 `node generate_once.js` 를 띄운다(정상 3.1~3.8초, 제한 15초). 물리 코어 1개에서 부하가 겹치면 초과 → 메타·자막 요청 전체 실패. → T-007
- **조치**: **상주 HTTP 서버**(`build/main.js`, `127.0.0.1:4416`)로 전환. 실행 스크립트가 첫 창으로 띄우고 4416 잔존 프로세스도 정리한다. 자막 경로 **22초 → 6초**, 타임아웃 사라짐.
- **부수 수정**: flat 목록의 제목이 잘린 채 저장돼 복사 머리말의 제목이 잘려 AI 에게 전달됐다. 메타 보충에서 전체 제목으로 덮어쓴다.
- 프로토타입 커밋 `7f4a21d` (브랜치 `prototype/e2e-slice`, `main` 병합 안 함).

## 2. 방법론 v2 — 산출물 효율 검토

사용자 요청: "초기 설정한 산출물들이 너무 비효율적이지 않은지 검토" + 목표 5가지(완성된 프로그램 / 유지보수 용이 / 인수인계 / 사용자 이해 / 트러블슈팅 근거).
이어서 "불필요한 subagent 없애줘", "프롬프트도 생성할 필요가 있을까? 내가 요청한 내용만 적혀있으면 되지 않을까?"

### 측정

| 항목 | 값 |
|---|---|
| Phase 0 문서 : 코드 | 4,036줄 : 1,127줄 |
| Phase 마다 반복 생산되는 문서 | 2,047줄 × 남은 6 Phase ≈ 12,000줄 |
| "Whisper 기본 모델 = small" 이 적힌 파일 | 11개 |
| 정합성 검사가 찾은 어긋남 | 31건 — 대부분 우리 문서끼리 불일치 |
| E2E 스모크 테스트 | 0개 |

진단: 낭비의 뿌리는 **중복**이다. 사실 하나가 11곳에 있으면 고칠 곳이 11곳이 되고, 그래서 검사를 돌리고, 검사가 찾은 것을 또 고친다.

### 사용자 결정

- 조정 범위: **제안 전부 적용**
- Phase 1~3 구현 방식: **프로토타입을 정식 구조로 이관** (기술 검증 되풀이 안 함)

### 적용

| 변경 | 내용 |
|---|---|
| Phase 당 문서 6개 → **3개** | 설계서(요구사항 흡수) · 실측기록(TechSpike+테스트결과서+SelfReview 통합) · 학습가이드 |
| **단일 출처 규칙 신설** (`CLAUDE.md` 5절) | 실행 파라미터는 설정 파일, 실측값은 실측기록, 규칙은 CLAUDE.md … 나머지는 링크. 예외는 CLAUDE.md 0절 하나 |
| 서브에이전트 폐지 | `doc-consistency` 삭제 + `설계서_Agents.md` 삭제. 대신 5.1절 `grep` 절차. 스킬 `troubleshoot` 은 유지 |
| 작업 프롬프트 폐지 | `docs/prompts/` 디렉토리 삭제. 요청 원문·결정은 `docs/internal/qa/P0_질의응답_초기요구.md` 로 (원본은 git 이력) |
| DocType 5종 → 4종 | 설계서·실측기록·학습가이드·검토서 |
| 템플릿 6종 → 3종 | 601줄 → 388줄 |
| **E2E 스모크 필수화** | Phase 1부터. Phase 마다 그 Phase 의 흐름을 검증하는 E2E 최소 1개 |
| **사용자용 문제 해결 신설** | `docs/internal/학습가이드_문제해결.md` — 화면 증상 → 확인 → 조치 |
| 프로토타입 문서 `main` 으로 | `검토서_Prototype`·트러블슈팅 T-005~007·qa 2건·history 2건. 코드는 브랜치에 그대로 |
| 기존 중복 정리 | scope 5.3·8.1, Architecture 5.1, 용어집, CLAUDE 보류 2 의 값을 링크로. `verify_docs` 환경 디렉토리 오탐도 수정 |

결과: 문서 24개 4,036줄 → **19개 3,742줄**(내용은 프로토타입 기록·문제해결 문서만큼 늘었다). Phase 당 2,047줄 → 약 900줄.

## 3. 검증

worktree 에는 `backend/.venv` 가 없어 환경 의존 테스트 2건이 실패한다 — 본 체크아웃에서 병합 후 재검증한다.
`scripts/verify_docs.py` 는 worktree 에서 문서 21개 위반 0.

## 종료 시점 상태

- `chore/methodology-v2` 커밋·push 후 `main` 병합 예정. worktree `D:\claude\ytl-method` 는 병합 후 제거한다.
- 프로토타입 서버(4416·8765·5173)는 이 세션이 시작한 것이라 세션 종료 시 함께 내려간다.

## 다음 단계

1. `main` 병합 → 본 체크아웃에서 pytest·verify_docs·프론트 재검증.
2. Phase 1 착수(`impl-phase1`): `P1_설계서_ChannelListing` (프로토타입 `proto/yt.py`·`store.py` 를 `youtube/`·`repository/` 로 이관하는 설계) → 코드+테스트+E2E → `P1_실측기록_ChannelListing` → 학습가이드.
3. Phase 1 에서 함께 할 것: `check_env` 에 PO 토큰 서버 검사 추가, `verify_docs` 에 `.ps1` BOM 검사(T-005), 목록 제목은 잠정값·메타 보충이 정본(T-007 부수 발견).

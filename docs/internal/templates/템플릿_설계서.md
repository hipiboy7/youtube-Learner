# P<N> 설계서 — <Topic>

- 상위 문서: `docs/P<N>_요구사항정의서_<Topic>.md` (v<k>), `docs/설계서_Architecture.md` (v<k>) <n>절
- 규칙: `CLAUDE.md` 4절 2단계 산출물. 다음 산출물은 코드+테스트(3절 등급 기준, A는 테스트 먼저) → `docs/P<N>_테스트결과서_<Topic>.md`
- 작성일: YYYY-MM-DD / 작성 LLM: <모델>
- 상태: 초안 | 승인(YYYY-MM-DD) | 개정 v<k>

> 안내: 설계서는 "어떻게"에 답한다. 모듈 1개 = 절 1개. 각 절은 **대응 FR → 인터페이스 → 설계 판단 → 검증 방법** 순서를 지킨다.
> "설계 판단"이 이 문서의 핵심이다. 모든 결정을 **왜 이것인가 + 기각한 대안 + 기각 이유**로 쓴다. 이유를 쓸 수 없는 결정은 다시 본다.

## 0. 모듈 구성 및 의존 방향

```
youtube_learner/
├── <pkg>/
│   ├── <module_a>.py     # [P<N>] <역할>            등급 A
│   └── <module_b>.py     # [P<N>] <역할>            등급 B
└── domain/interfaces.py  # [P0]  <이 Phase가 구현하는 Protocol>

의존 방향:  cli → <pkg>/<module_b> → domain.interfaces ← <pkg>/<module_a>
            (상위는 Protocol에만 의존. 구체 구현은 조립 지점에서 주입 — CLAUDE.md 2절 DIP)
```

## 1. `<pkg>/<module_a>.py` — <역할>

**대응**: FR-<n>, FR-<n+1>

### 1.1 인터페이스

```python
def <function>(<arg>: <Type>, ...) -> <ReturnType>:
    """<한 줄 역할>. 등급 A.

    Raises:
        <Exception>: <조건>
    """
```

| 입력 | 출력 | 예외 | 부작용 |
|---|---|---|---|
| <타입·제약> | <타입> | <어떤 조건에 무엇> | 없음 (순수) / <파일·DB·네트워크> |

### 1.2 설계 판단

- **왜 <선택>인가** — <이유. 상위 문서·TechSpike 실측 근거>.
  → 대안 <A>도 가능하지만 <이유>로 기각. 대안 <B>는 <이유>로 기각.
- **왜 <경계를 여기에 두나>** — <변경 가능성이 높은 축이 무엇인지>.
- **실측으로 확인한 사실** — <TechSpike 3절 인용. 확인 일자>. 문서만 믿고 단정하지 않는다.

### 1.3 검증 방법

| 등급 | 테스트 파일 | 확인하는 것 |
|---|---|---|
| A | `backend/tests/unit/test_<module_a>.py` | 정상 케이스 + **거부 케이스**(부적합 입력을 거부하는가) |

## 2. `<pkg>/<module_b>.py` — <역할>

**대응**: FR-<n+2>

### 2.1 인터페이스
### 2.2 설계 판단
### 2.3 검증 방법

## <N>. 데이터 (해당 시)

> 안내: 테이블·컬럼·인덱스, 파일 산출물 명명 규칙, 보존 원칙(원본 보존·하류 덮어쓰기 금지), 동반 저장하는 버전 필드.

| 테이블/파일 | 키 | 필드 | 보존 규칙 |
|---|---|---|---|
| <name> | <자연키> | <필드…> | <원본 보존 / 재생성 가능 / 버전 동반> |

## <N+1>. 외부 연동 (해당 시)

> 안내: 실제로 호출해 확인한 사실만 쓴다. 확인 일자·yt-dlp 버전 등 "언제 무엇으로 확인했나"를 함께 적는다.

| 대상 | 호출 | 확인한 사실 | 확인 일자 |
|---|---|---|---|
| <서비스> | <명령/옵션> | <사실> | YYYY-MM-DD |

## <N+2>. 설정값 배치 (하드코딩 금지 — CLAUDE.md 5절)

| 값 | 위치 | 이유 |
|---|---|---|
| <값> | `.env` / `constants.py` / `backend/config/<name>.json` | <환경별 / 설계 고정 / 실행별 조절> |

## <N+3>. 구현 순서

| 순서 | 작업 | 등급 | 테스트 먼저? | 산출물 |
|---|---|---|---|---|
| 1 | <A등급 순수 로직> | A | 예 (Red→Green) | `<경로>` |
| 2 | <B등급 오케스트레이션> | B | 아니오 (구현 후 통합 테스트) | `<경로>` |
| 3 | 실연동 확인 | — | — | 테스트결과서 5절 |

## <N+4>. ⚠️ 다음 Phase 인계

| 항목 | 내용 | 받는 Phase |
|---|---|---|
| <인터페이스/데이터/미결> | <내용> | Phase <M> |

## <N+5>. 관련 문서

| 문서 | 관계 |
|---|---|
| `docs/P<N>_요구사항정의서_<Topic>.md` | 상위 — FR 정의 |
| `docs/설계서_Architecture.md` <n>절 | 상위 — 전체 골격·DIP 경계 |
| `docs/internal/P<N>_검토서_TechSpike.md` | 근거 — 실측 |

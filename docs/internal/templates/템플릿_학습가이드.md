# P<N> 학습가이드 — <Topic>

- 독자: 컴퓨터공학 학부 졸업 수준, 이 프로젝트를 처음 보는 사람 (`docs/internal/용어집.md` 기준)
- 작성일: YYYY-MM-DD / 작성 LLM: <모델>
- 규칙: `CLAUDE.md` 1절 7단계. 내부 자료(9절 경계). 실습은 **사본 디렉토리**에서만 — 실제 `data/`·DB를 건드리지 않는다
- 대응 산출물: `docs/P<N>_요구사항정의서_<Topic>.md`, `docs/P<N>_설계서_<Topic>.md`, `docs/P<N>_테스트결과서_<Topic>.md`

> 안내: 목적은 담당자(사람)가 이 Phase 산출물을 **스스로 재현하고 설명**할 수 있게 하는 것이다.
> "무엇을 왜 그렇게 만들었나"와 "직접 확인하는 방법"이 중심이다. 코드를 다시 설명하는 문서가 아니다.

## 0. 시작 전 확인 (매번)

```powershell
.\scripts\check_env.ps1        # ==> READY 가 나와야 한다
```

READY가 아니면 이 가이드의 실습을 시작하지 않는다. 무엇이 빠졌는지는 스크립트 출력이 말해준다.

## 0.5 먼저 알아야 할 것 <n>가지

| # | 용어 | 한 줄 | 용어집 |
|---|---|---|---|
| ① | <용어> | <한 줄 설명> | `docs/internal/용어집.md` |
| ② | <용어> | <…> | |

## 1. 이 Phase가 한 일 — 한 장 요약

```
<입력> ──▶ [<모듈 A>] ──▶ [<모듈 B>] ──▶ <산출물>
              │
              └─ <외부 서비스> (실호출)
```

<3~5줄. 어떤 문제를 어떤 순서로 풀었나.>

## 2. 직접 실행해 보기

### 2.1 실습 사본 만들기

```powershell
<사본 디렉토리 생성·환경변수(DATA_DIR 등) 지정 명령>
```

### 2.2 <첫 실습 — 가장 작은 단위 1건>

```powershell
<명령>
```

기대 출력: <…>. 다르게 나오면 → 4절 또는 `검토서_트러블슈팅.md`.

### 2.3 <두 번째 실습 — 멱등성 확인: 같은 명령 2회>

### 2.<n> 실습 정리

```powershell
<사본 삭제 명령>
```

## 3. 설계 결정 <n>개 — 리뷰에서 설명할 것

| # | 결정 | 기각한 대안 | 왜 | 어디에 적혀 있나 |
|---|---|---|---|---|
| 1 | <결정> | <대안> | <이유 한 줄> | `docs/P<N>_설계서_<Topic>.md` <n>.2절 |

## 4. 실제가 문서와 다르게 동작한 것들

> 안내: TechSpike·실연동에서 발견한 것. 다음 사람이 같은 함정에 빠지지 않게.

| # | 문서/예상 | 실제 | 우리가 한 일 |
|---|---|---|---|
| 1 | <…> | <…> | <…> |

## 5. 자체 점검에서 찾은 내 실수

<SelfReview 1절 요약 — 왜 그 실수가 생겼는지 구조를 설명. 링크: `docs/internal/P<N>_검토서_SelfReview.md`>

## 6. 리뷰에서 나올 수 있는 질문과 답

**Q. <질문>**
A. <답 — 근거 문서 절번호 포함>

**Q. <질문>**
A. <…>

## 7. 읽을 문서 순서

1. `CLAUDE.md` 0절·1절 (규칙·보류 결정)
2. `docs/scope-definition.md` <n>절
3. `docs/P<N>_요구사항정의서_<Topic>.md`
4. `docs/P<N>_설계서_<Topic>.md`
5. 코드 (`backend/src/youtube_learner/<pkg>/`) — docstring 첫 줄이 설계서 절을 가리킨다
6. `docs/P<N>_테스트결과서_<Topic>.md`

## 8. 다음 Phase로 넘어갈 때 기억할 것

- <인계 사항 — 테스트결과서 8절>
- <보류 결정 번호 — CLAUDE.md 1절 표 <k>>

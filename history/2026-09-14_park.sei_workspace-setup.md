# 작업 히스토리: 프로젝트 기획 → Phase 0 (범위 정의 + 공통 모듈) 구현 완료, Direct 리뷰 대기

- 일자: 2026-09-14
- 작업자: park.sei (LLM: Fable 5.1)
- 브랜치: `main`(초기화) → `impl-phase0` (현재, `origin` 추적)
- 원격: `https://github.com/hipiboy7/youtube-Learner.git`

## 목적

"원하는 유튜브 채널로 학습하는 프로그램"을 만들기 위한 기획을 세우고, 사용자의 기존 프로젝트 방법론(`SDS-MW-Team/embedding-based-llm-classifier`)을 따를 수 있는지 판정한 뒤, Phase 0 를 그 방법론대로 수행한다.

## 수행 내용

### 1. 기획 (plan mode, 승인 2026-09-14)

- 사용자 결정: FastAPI 백엔드 · 로컬 faster-whisper(CPU) · React → Tauri → Capacitor · **요약은 슬롯만**(추출 완성·배포 후 P5) · 한국어 채널 수백 개 → 수천 개 확장 구조.
- 방법론 판정: 참조 저장소를 복제해 `CLAUDE.md`·워크플로·템플릿·코드 규약·에이전트를 분석. **"따를 수 있다, 다섯 곳만 조정"** — 모노레포, 제품 기능 기반 Phase(P0~P6), "완성본 참조"→"기술 검증(TechSpike)", Windows/PowerShell, 심사 전용 산출물 제외. 문서 수준은 참조와 동일, 혼자 진행(PR 없음), 개인 GitHub.
- 기술 검증(Plan 에이전트, 웹 조사): yt-dlp 탭 URL·PO 토큰(bgutil)·JS 런타임(Node), faster-whisper CPU 휠, Huey(SQLite), Tauri 사이드카(onedir), Capacitor.
- 기획서는 `docs/prompts/phase0/project-plan-v1.md` 로 보존.

### 2. 저장소 초기화

`git init -b main` → `.gitignore`·`.gitattributes`·README 초안 첫 커밋 → `impl-phase0` 분기 → 사용자가 만든 원격 연결·push. 사용자 규칙 추가: **커밋하면 항상 push**(`CLAUDE.md` 11절).

### 3. TechSpike (프롬프트 작성 전 실측) — `docs/internal/P0_검토서_TechSpike.md`

| 확인 | 결과 |
|---|---|
| 탭 flat 목록 | 60건/2.4초. 숏폼 flat 에 `duration` 없음, 두 탭 `upload_date` 없음 → 메타 보충 필수 |
| 자막 | ko json3 745 이벤트. 원어 트랙 `ko-orig`. **`en` 요청 → 429**(번역), 한 언어 실패 시 명령 전체 실패 |
| PO 토큰 | pip 만으로는 unavailable → 서버 디렉토리 빌드 후 동작 (T-001) |
| 오디오 | ffmpeg 없이 m4a 2.3MB/6.2초, PyAV 디코딩 |
| Whisper (코어 1) | small 3.05x / large-v3-turbo 0.86x. 기본 small (보류 결정 2) |
| 폴백 | youtube-transcript-api 1.6초 동작 |
| 환경 | 물리 코어 1, 디스크 C·D 각 5GB 미만, Rust 없음 (T-002, 보류 결정 8) |

### 4. Phase 0 산출물

- 상위 문서 3종: `docs/scope-definition.md` v1, `docs/설계서_Architecture.md` v1, `CLAUDE.md` v1(보류 결정 8건).
- 방법론 도구: 템플릿 6종, `doc-consistency` 에이전트·`troubleshoot` 스킬 이식, `설계서_Agents`, `용어집`, 트러블슈팅 T-001~003, internal README, qa README.
- 4단계: `P0_요구사항정의서_Common`(FR-1~34) → `P0_설계서_Common` → 코드+테스트 → `P0_테스트결과서_Common`.
- 코드: `constants`·`exceptions`·`domain/{models,interfaces}`·`config`·`logging_config`·`workflow/run_context`·`repository/db`·`analysis/{null_analyzer,registry}`·`cli/check_env` + `scripts/check_env.ps1`·`scripts/verify_docs.py` + `frontend/`(Vite+React+TS, `formatTimestamp`·API URL).
- 테스트: 등급 A 120건(테스트 먼저 — Red 5 파일 수집 실패 → Green), 등급 B 106건, 프론트 13건 — 합계 226. 커버리지 A 98.8% / B 95.6% / 총 97%, `verify_docs` 85%→97%.
- `check_env.ps1` → **`==> READY`** (WARN: 디스크, 모델 캐시 → small 캐시를 `data/models` 로 이동해 OK).

### 5. 발견·결정

- Whisper small 캐시(464MB)를 C: 기본 HF 캐시에서 프로젝트 `HF_HOME`(D:)으로 이동. turbo 캐시(1.6GB)는 측정 후 삭제.
- ruff 줄 길이 120 → **140** (한국어 메시지 23건이 의미 없는 줄바꿈 강요). `pyproject.toml` 에 근거.
- `verify_docs` 역테스트가 `…\python.exe` 전체 경로 명령 인식 누락을 잡아 수정 — 역테스트의 존재 이유.
- 설계서와 구현의 차이 2건(`run_context(run_id=)` 인자, `make_engine` 이 `data_dir` 생성) → SelfReview 에서 설계서 정합.
- doc-consistency 첫 실행(sonnet): 지적 9건 → 7건 반영. 메인 재검토에서 놓침 2건(숫자 대조) → 규칙대로 opus 승격.
- **사용자 규칙·결정(Direct 리뷰 중)**: (1) "로컬 디스크는 항상 D: 우선" → `CLAUDE.md` 7절, bgutil 을 홈(C:)→저장소 `tools/`(D:), `Settings.bgutil_script_path` 기본 `project_root()/tools/…`, pip/npm 캐시 `D:\claude\.cache\`, C: 캐시(pip 116MB·npm 321MB·HF) 삭제. (2) 디스크 부족 시 **D: 증설**(보류 결정 8 판정 재료). (3) **v1 에 수동 요약 흐름 추가** — 스크립트 원클릭 복사 + 외부 AI 챗 결과를 붙여넣어 저장하는 '요약 및 정리' 메뉴 → scope v2·프롬프트 v2·qa 기록.

## 종료 시점 상태

- 브랜치 `impl-phase0`, `origin` 과 동기화(마지막 커밋은 아래 4~5 항목 이후 push).
- Phase 0 완료 기준(요구사항정의서 7절) 전건 충족 — 상세는 `docs/P0_테스트결과서_Common.md` 7절.
- **Direct 리뷰 대기.** 병합(`merge --no-ff main` → push)은 사용자 리뷰 후.

## 다음 단계

1. 사용자 Direct 리뷰 → 지적 반영(Phase 0 부터 전체 재독, `doc-consistency`) → `git merge --no-ff impl-phase0` → push. 브랜치 보존.
2. Phase 1 착수: `impl-phase1` 분기 → **P1 TechSpike**(채널 전량 목록화·숏폼 100+ 페이지네이션·메타 보충 속도·`curl_cffi` 효과) → 프롬프트 `docs/prompts/phase1/channel-listing-v1.md` → 4단계.
3. 보류 결정 확인: #3(탭 판정, P1 전량 재확인), #8(디스크 — P4 전).
